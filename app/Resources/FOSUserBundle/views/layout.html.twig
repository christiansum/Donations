<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title> Niloket - {% block title %}Pagina Inicial{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('bundles/main/css/jquery-ui.min.css') }}">
            <link rel="stylesheet" href="{{ asset('bundles/main/css/jquery-ui.structure.min.css') }}">
            <link rel="stylesheet" href="{{ asset('bundles/main/css/bootstrap.min.css')}}" >
            <link rel="stylesheet" href="{{ asset('bundles/main/css/datepicker3.css')}}" >
            <link rel="stylesheet" href="{{ asset('bundles/main/css/styles.css')}}" >
            <script src="{{ asset('bundles/main/js/lumino.glyphs.js')}}"></script>
            
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div>
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                <a href="{{ path('fos_user_security_logout') }}">
                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                </a>
            {% else %}
                <!--<a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>-->
            {% endif %}
        </div>

        {% for type, messages in app.session.flashBag.all %}
            {% for message in messages %}
                <div class="{{ type }}">
                    {{ message|trans({}, 'FOSUserBundle') }}
                </div>
            {% endfor %}
        {% endfor %}

        <div>
            {% block fos_user_content %}
            {% endblock fos_user_content %}
        </div>
        <script src="{{ asset('bundles/main/js/jquery-3.1.1.slim.min.js') }}"></script>
        <script src="{{ asset('bundles/main/js/jquery-ui.min.js')}}"></script>
        <script src="{{ asset('bundles/main/js/bootstrap.min.js')}}"></script>
        <script src="{{ asset('bundles/main/js/chart.min.js')}}"></script>
        <script src="{{ asset('bundles/main/js/chart-data.js')}}"></script>
        <script src="{{ asset('bundles/main/js/easypiechart.js')}}"></script>
        <script src="{{ asset('bundles/main/js/easypiechart-data.js')}}"></script>
        <script src="{{ asset('bundles/main/js/bootstrap-datepicker.js')}}"></script>
    </body>
</html>
